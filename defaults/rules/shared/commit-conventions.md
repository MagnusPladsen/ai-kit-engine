---
description: Commit message conventions — triggers when committing code
alwaysApply: true
---

<!-- abaris-ai-kit | Do not edit - managed by install.sh | Run install.sh --check to verify -->

# Commit Conventions

- Always use the `/commit` skill when committing — never create ad-hoc commits
- A user request to commit counts as explicit permission
- If user says "commit and push", push after. If just "commit", ask whether to push
- Never use `git add -A` — always stage specific files
