name = "Minimal"
description = "Shared rules + commit skill only"
stacks = []

[rules]
items = ["commit-conventions", "no-auto-push", "plan-refinement", "security",
         "code-quality", "naming-conventions", "no-phantom-code", "minimal-diff"]

[skills]
items = ["commit", "review"]

[plugins]
items = []
